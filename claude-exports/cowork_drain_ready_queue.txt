You are operating under prompts/cowork/master_operator.md and the
apply_to_job_from_packet skill at prompts/cowork/skills/apply_to_job_from_packet/.
Follow those as your primary contract. This run overrides the submit_policy
toward manual approval for every packet.

Goal: drain data/queues/ready_to_apply/ by applying to every packet in it,
one at a time, pausing before each submit for my explicit approval.

Procedure — repeat until ready_to_apply/ is empty:

1. List data/queues/ready_to_apply/*.json. If empty, stop and give me the
   final summary (see step 8). Otherwise pick the oldest file (lexicographic
   by filename works; packet IDs are content-addressed so order is stable).

2. Transition the packet to in_progress:
     venv/bin/python scripts/queues/transition_packet.py <packet_id> in_progress
   Read the packet JSON from its new location in data/queues/in_progress/.

3. Load referenced files: resume_path, applicant_answers_path,
   job_snapshot_path, screening_decision_path, and cover_letter_path if
   cover_letter_status == "approved". If anything required is missing, skip
   straight to step 7 with result=failed and notes describing what was missing.

4. Open apply_url, navigate the ATS flow per ats_playbooks.md, and fill
   fields using ONLY the packet, applicant_master_answers.md, and the
   approved cover letter. Honor escalation_policy.md — if an immediate
   escalation trigger fires, skip to step 7 with the appropriate
   waiting_* result; do not pause for my input on escalations.

5. When you reach the final submit button (but have not clicked it), run
   the full submission_audit_checklist.md. If any check fails, skip to
   step 7 without submitting.

6. STOP and print exactly:

     ── APPROVAL NEEDED ──
     Packet: <packet_id>
     Company: <company>
     Title: <title>
     ATS: <ats_type>
     URL: <current_url>
     Cover letter: <attached|none|not required>
     Ready to submit. Reply "approved" to submit, "skip" to skip this
     packet (moves to waiting_for_human_review), or "abort" to stop the
     whole queue.

   Then wait for my reply. Do not click anything until I respond.

   - "approved" → click submit, capture the confirmation number if
     visible, proceed to step 7 with result=submitted.
   - "skip" → do not submit. Proceed to step 7 with
     result=waiting_for_human_review and notes explaining I deferred it.
   - "abort" → write the run log for the current packet with
     result=failed and notes="aborted by user"; transition it to
     waiting_for_human_review; then stop the loop and give me the
     summary in step 8.

7. Finalize the packet:
   - Write a RunLog JSON to data/run_logs/<run_id>.json matching
     schemas/run_log.schema.json (fields: run_id, packet_id,
     worker="cowork", started_at, finished_at, result, confirmation_number,
     issue_type, notes).
   - If escalated, also write an InterventionReport to
     data/run_logs/interventions/ matching
     schemas/intervention_report.schema.json.
   - Transition the packet to its terminal queue:
       submitted                    → completed
       waiting_for_signup           → waiting_for_signup
       waiting_for_human_review     → waiting_for_human_review
       waiting_for_cover_letter     → waiting_for_cover_letter_approval
       failed / expired_posting     → failed
     using scripts/queues/transition_packet.py. Confirm the source file
     in in_progress/ is actually gone after the transition — if it is
     not, tell me immediately and stop.

8. Loop back to step 1.

When the queue is drained (or I aborted), print a summary table: one
row per packet with packet_id, company, title, result, and
confirmation number if any.

Constraints:
- Do not invent field answers. Escalate per escalation_policy.md instead.
- Do not create accounts, solve captchas, or handle OTP.
- Never submit without my "approved" reply for the current packet —
  even if submit_policy.auto_submit_allowed is true in the packet.
- Move on after each packet regardless of outcome; a single failure or
  escalation must not stop the queue unless I say "abort".

Scope of writes — strictly enforced:
- You may only create or modify files under:
    data/queues/
    data/run_logs/
    data/run_logs/interventions/
- You may NOT edit, create, or delete any file under scripts/, schemas/,
  prompts/, config/, tests/, docs/, n8n/, artifacts/, or anywhere else
  in the repo. That includes "small fixes," workarounds, or patches you
  think are obviously correct. The codebase is out of scope for this run.
- If a script (e.g. transition_packet.py) fails with a permission error,
  sandbox error, FUSE/mount error, or any other environment-level issue:
  STOP the loop, tell me the exact command, the exact error message, and
  the current packet's state, and wait for me to fix it. Do not edit the
  script to work around it. Do not invent a tombstone/marker/flag scheme
  to bypass the failure. Do not retry with different code.
- If you believe a script is genuinely buggy (not an environment issue),
  still do not edit it — report it to me and stop.
